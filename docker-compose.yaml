services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visitor-management-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    networks:
      - visitor-network

networks:
  visitor-network:
    driver: bridge
