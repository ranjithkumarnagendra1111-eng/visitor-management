Reads data/employees.csv (20 records).
Writes all employees into data/employees.json.
Creates a temporary JSON file with the same data (shows temp file handling).
Returns a success message and path to the temp file.